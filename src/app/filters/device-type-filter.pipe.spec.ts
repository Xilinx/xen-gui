import { DeviceTypeFilterPipe } from './device-type-filter.pipe';

describe('DeviceTypeFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new DeviceTypeFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
