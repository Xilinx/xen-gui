import { DeviceNameFilterPipe } from './device-name-filter.pipe';

describe('DeviceNameFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new DeviceNameFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
