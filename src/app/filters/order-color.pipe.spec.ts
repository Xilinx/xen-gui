import { OrderColorPipe } from './order-color.pipe';

describe('OrderColorPipe', () => {
  it('create an instance', () => {
    const pipe = new OrderColorPipe();
    expect(pipe).toBeTruthy();
  });
});
